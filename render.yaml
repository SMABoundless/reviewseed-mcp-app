services:
  - type: web
    name: reviewseed-mcp-app
    runtime: node
    buildCommand: npm install && npm run build
    startCommand: node dist/main.js
    plan: free
    envVars:
      - key: NODE_ENV
        value: production
